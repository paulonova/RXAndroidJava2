package tutorial;

public interface io.reactiveJava.Observer {
	
	void register(io.reactiveJava.Observable observer);

	void unregister(io.reactiveJava.Observable observer);
	
	// New tutorial published to inform all subscribed users
	 void notifyAllAboutChange();
    
    

}
